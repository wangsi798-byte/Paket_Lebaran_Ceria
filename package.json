{
  "name": "paket-lebaran-ceria-unified",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/src/server.js",
    "build": "cd frontend && npm install && npm run build",
    "vercel-build": "echo 'Vercel build completed'",
    "test": "echo 'No root tests' && exit 0",
    "lint": "echo 'No root lint' && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^8.1.1",
    "multer": "^1.4.5-lts.1",
    "prom-client": "^15.1.0",
    "winston": "^3.8.2"
  }
}
